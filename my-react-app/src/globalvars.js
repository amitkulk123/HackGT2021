import React, { Component } from "react";
import { render } from "react-dom"


class global extends Component {
    // static numPoints = 0;
    // static subtotal = 0;
    // global.numPoints = 20;
    // global.subtotal = 0;
}
